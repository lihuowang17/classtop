# Optional dependencies for API Server
# Install with: pip install -r requirements-api.txt

# API Server Framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# Data validation (usually included with FastAPI)
pydantic>=2.0.0
